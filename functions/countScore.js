// export default function countScore(palabra) { //esto cuenta el largo de cada palabra del array lista, y suma puntos al score.
    
//       const length = palabra.length;

//       if        (length === 3)  {score ++
//       } else if (length === 4)  {score ++
//       } else if (length === 5)  {score += 2
//       } else if (length === 6)  {score += 3
//       } else if (length === 7)  {score += 5
//       } else if (length  >  7)  {score += 10
//       }
//       console.log(score)
//   }